machine:
  node:
    version: v4.1.1

dependencies:
  post:
    #
      - npm install -g gulp
      - npm install -g bower
      - bower install
      - gulp build

deployment:
  production:
    branch: master
    heroku:
      appname: sanai

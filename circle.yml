machine:
  node:
    version: v5.11.1

dependencies:
  post:
    - npm install -g gulp
    - npm install -g bower
    - bower install
    - gulp build

deployment:
  production:
    branch: master
    heroku:
      appname: sanai

machine:
  node:
    version: v8.11.0

dependencies:
  post:
    #
      - npm install -g gulp
      - npm install -g bower
      - bower install
      - gulp build

deployment:
  production:
    branch: master
    heroku:
      appname: sanai
